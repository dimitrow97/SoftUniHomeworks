function endsWith(string, subStr) {
    let result = string.endsWith(subStr);
    console.log(result);
}

endsWith('The new iPhone has no headphones jack.', 'o headphones jack.');
endsWith('The new iPhone has no headphones jack.', 'O headphones jack.');